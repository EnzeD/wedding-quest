{
  "name": "wedding-quest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.31.1",
    "@types/three": "^0.183.1",
    "typescript": "^6.0.2",
    "vite": "^6.3.0",
    "wrangler": "^4.81.0"
  }
}
